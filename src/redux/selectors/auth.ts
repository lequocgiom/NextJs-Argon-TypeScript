// import { StoreState } from '../types';

// export const authSelector = (state: StoreState) => state.auth;
// export const accountListSelector = (state: StoreState) => state.auth.accounts;
// export const accountSelector = (state: StoreState) => state.auth.account;
export {};
